{
  "name": "firered-research-notebook",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first documentation and formatting-review companion for FireRed source-level research. Documentation and validation only — never generates game content.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "audit:network": "node scripts/check-no-network.mjs"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9"
  }
}
