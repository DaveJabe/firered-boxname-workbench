// Hand-written declaration for the plain-JS script of the same name — see
// check-no-local-generator-artifact.d.mts for why this exists instead of
// converting the script itself to TypeScript.
export declare function findArtifactPaths(paths: readonly string[]): string[];
